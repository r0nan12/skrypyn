class SearchController < ApplicationController
def index
  @search_param = params[:q]

    if @search_param
      render :json => {:success => true, :text => @search_param } and return
    else
      render  :json =>{:success => false, :text => "SRAKA!!!"} and return

end
  end
end
