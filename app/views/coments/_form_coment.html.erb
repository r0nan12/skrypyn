<h2>Add a comment:</h2>
<%= form_for([@article, @article.coments.build]) do |f| %>

    <p>
      <%= f.label :body %><br>
      <%= f.text_area :text %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>