<h1>Editing comment</h1>
 
<%= form_for  [@article, @coment] do |f| %>

    <p>
      <%= f.label :body %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.submit %>
    </p>
    <%= link_to 'Back', article_path(@article) %>
<% end %>
 

